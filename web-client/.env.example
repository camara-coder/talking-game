# Voice Service API Configuration
#
# For local development:
# - Leave these UNSET to use Vite's proxy (recommended)
# - This avoids CORS issues by proxying requests through the dev server
#
# For production:
# - Set these to your deployed backend URLs
#
# VITE_API_BASE_URL=http://127.0.0.1:8008
# VITE_WS_BASE_URL=ws://127.0.0.1:8008
